[project]
name = "fastapi-managed-image-fixture"
version = "0.1.1"
dependencies = [
  "fastapi==0.116.0",
  "uvicorn==0.32.1"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"