# Expected outcome: pipeline succeeds, logs are printed, and output/hello.txt is produced.
version: 1

pipeline:
  name: success-basic
  image: alpine:latest

steps:
  - name: run
    command: ./scripts/run.sh

artifacts:
  paths:
    - output/**