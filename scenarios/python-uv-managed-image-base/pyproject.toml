[project]
name = "fastapi-managed-image-fixture"
version = "0.1.0"
dependencies = [
  "fastapi==0.115.0",
  "uvicorn==0.32.0"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"